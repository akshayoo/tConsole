import {LoginComp} from './loginComp'

export const metadata = {
    "title" : "Login- tConsole",
    "description" : "tConsole login"
}

export default function page(){
    return(
        <LoginComp />
    )
}